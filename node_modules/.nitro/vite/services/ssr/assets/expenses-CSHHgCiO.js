import { l as PAY_METHODS, p as useWorkshop, r as PageTitle, s as EXPENSE_TYPES, y as toman } from "./router-BUaE4XGK.js";
import { n as Input, r as Select, t as Field } from "./input-CHwMk_IU.js";
import { n as DataTable, t as AddForm } from "./journal-KWw6p0QH.js";
import { jsx, jsxs } from "react/jsx-runtime";
//#region src/routes/expenses.tsx?tsr-split=component
function Page() {
	const { expenses, employees, addExpense, remove, settings } = useWorkshop();
	return /* @__PURE__ */ jsxs("div", { children: [
		/* @__PURE__ */ jsx(PageTitle, {
			title: "هزینه‌ها",
			hint: "حقوق را اول ماه بزنید. هر هزینه یک ردیف."
		}),
		/* @__PURE__ */ jsxs(AddForm, {
			title: "ثبت هزینه",
			onSubmit: (e) => {
				const f = new FormData(e.currentTarget);
				addExpense({
					date: String(f.get("date") || settings.today),
					type: String(f.get("type")),
					amount: Number(f.get("amount")),
					payer: String(f.get("payer")),
					method: String(f.get("method")),
					note: String(f.get("note") || "")
				});
			},
			children: [
				/* @__PURE__ */ jsx(Field, {
					label: "تاریخ",
					children: /* @__PURE__ */ jsx(Input, {
						name: "date",
						defaultValue: settings.today
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "نوع",
					children: /* @__PURE__ */ jsx(Select, {
						name: "type",
						children: EXPENSE_TYPES.map((t) => /* @__PURE__ */ jsx("option", { children: t }, t))
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "مبلغ (تومان)",
					children: /* @__PURE__ */ jsx(Input, {
						name: "amount",
						type: "number",
						required: true
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "پرداخت‌کننده",
					children: /* @__PURE__ */ jsx(Select, {
						name: "payer",
						children: employees.map((e) => /* @__PURE__ */ jsx("option", { children: e.name }, e.id))
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "روش",
					children: /* @__PURE__ */ jsx(Select, {
						name: "method",
						children: PAY_METHODS.map((m) => /* @__PURE__ */ jsx("option", { children: m }, m))
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "شرح",
					children: /* @__PURE__ */ jsx(Input, { name: "note" })
				})
			]
		}),
		/* @__PURE__ */ jsx(DataTable, {
			columns: [
				{
					key: "d",
					label: "تاریخ"
				},
				{
					key: "t",
					label: "نوع"
				},
				{
					key: "a",
					label: "مبلغ"
				},
				{
					key: "p",
					label: "پرداخت‌کننده"
				},
				{
					key: "m",
					label: "روش"
				},
				{
					key: "n",
					label: "شرح"
				}
			],
			rows: expenses.map((e) => ({
				id: e.id,
				onDelete: () => remove("expenses", e.id),
				cells: [
					e.date,
					e.type,
					toman(e.amount),
					e.payer,
					e.method,
					e.note
				]
			}))
		})
	] });
}
//#endregion
export { Page as component };
