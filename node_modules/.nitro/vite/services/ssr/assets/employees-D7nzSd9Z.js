import { p as useWorkshop, r as PageTitle, v as pct, y as toman } from "./router-BUaE4XGK.js";
import { n as Input, r as Select, t as Field } from "./input-CHwMk_IU.js";
import { n as DataTable, t as AddForm } from "./journal-KWw6p0QH.js";
import { m as saleFinal, s as inMonth } from "./kpis-CvtxtOcE.js";
import { jsx, jsxs } from "react/jsx-runtime";
//#region src/routes/employees.tsx?tsr-split=component
function Page() {
	const { employees, sales, addEmployee, remove, settings } = useWorkshop();
	return /* @__PURE__ */ jsxs("div", { children: [
		/* @__PURE__ */ jsx(PageTitle, {
			title: "کارکنان",
			hint: "جا برای رشد تا ۲۰ نفر. پورسانت ماه از فروش همان فروشنده × نرخ پورسانت."
		}),
		/* @__PURE__ */ jsxs(AddForm, {
			title: "افزودن نیرو",
			onSubmit: (e) => {
				const f = new FormData(e.currentTarget);
				addEmployee({
					name: String(f.get("name")),
					role: String(f.get("role")),
					duties: String(f.get("duties") || ""),
					salary: Number(f.get("salary") || 0),
					commissionRate: Number(f.get("commissionRate") || 0) / 100,
					start: String(f.get("start") || settings.today),
					status: "فعال",
					note: String(f.get("note") || "")
				});
			},
			children: [
				/* @__PURE__ */ jsx(Field, {
					label: "نام",
					children: /* @__PURE__ */ jsx(Input, {
						name: "name",
						required: true
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "سمت",
					children: /* @__PURE__ */ jsxs(Select, {
						name: "role",
						children: [
							/* @__PURE__ */ jsx("option", { children: "مدیر/مالک" }),
							/* @__PURE__ */ jsx("option", { children: "مسئول انبار" }),
							/* @__PURE__ */ jsx("option", { children: "نیروی تولید" }),
							/* @__PURE__ */ jsx("option", { children: "ویزیتور" }),
							/* @__PURE__ */ jsx("option", { children: "کمک تولید" }),
							/* @__PURE__ */ jsx("option", { children: "حسابدار" })
						]
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "وظایف",
					children: /* @__PURE__ */ jsx(Input, { name: "duties" })
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "حقوق ثابت",
					children: /* @__PURE__ */ jsx(Input, {
						name: "salary",
						type: "number"
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "پورسانت (٪)",
					children: /* @__PURE__ */ jsx(Input, {
						name: "commissionRate",
						type: "number",
						step: "0.1",
						defaultValue: 0
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "شروع",
					children: /* @__PURE__ */ jsx(Input, {
						name: "start",
						defaultValue: settings.today
					})
				})
			]
		}),
		/* @__PURE__ */ jsx(DataTable, {
			columns: [
				{
					key: "n",
					label: "نام"
				},
				{
					key: "r",
					label: "سمت"
				},
				{
					key: "d",
					label: "وظایف"
				},
				{
					key: "s",
					label: "حقوق"
				},
				{
					key: "c",
					label: "نرخ پورسانت"
				},
				{
					key: "cm",
					label: "پورسانت ماه"
				},
				{
					key: "t",
					label: "جمع ماه"
				}
			],
			rows: employees.map((e) => {
				const comm = sales.filter((s) => s.seller === e.name && inMonth(s.date, settings.year, settings.month)).reduce((a, s) => a + saleFinal(s), 0) * (e.commissionRate || 0);
				return {
					id: e.id,
					onDelete: () => remove("employees", e.id),
					cells: [
						e.name,
						e.role,
						e.duties,
						toman(e.salary),
						pct(e.commissionRate || 0),
						toman(comm),
						toman(e.salary + comm)
					]
				};
			})
		})
	] });
}
//#endregion
export { Page as component };
