import { d as QUALITY, g as num, i as APPROVAL, p as useWorkshop, r as PageTitle, u as PRODUCTS, y as toman } from "./router-BUaE4XGK.js";
import { n as Input, r as Select, t as Field } from "./input-CHwMk_IU.js";
import { n as DataTable, t as AddForm } from "./journal-KWw6p0QH.js";
import { f as purchaseTotal } from "./kpis-CvtxtOcE.js";
import { jsx, jsxs } from "react/jsx-runtime";
//#region src/routes/purchases.tsx?tsr-split=component
function Page() {
	const { purchases, suppliers, addPurchase, remove, settings } = useWorkshop();
	return /* @__PURE__ */ jsxs("div", { children: [
		/* @__PURE__ */ jsx(PageTitle, {
			title: "خرید مواد اولیه",
			hint: "فقط بار «تأیید شده» وارد موجودی و هزینه ماه می‌شود. قیمت تمام‌شده خودکار است."
		}),
		/* @__PURE__ */ jsxs(AddForm, {
			title: "ثبت خرید جدید",
			onSubmit: (e) => {
				const f = new FormData(e.currentTarget);
				addPurchase({
					date: String(f.get("date") || settings.today),
					supplier: String(f.get("supplier")),
					product: String(f.get("product")),
					kg: Number(f.get("kg")),
					price: Number(f.get("price")),
					freight: Number(f.get("freight") || 0),
					quality: String(f.get("quality")),
					status: String(f.get("status")),
					note: String(f.get("note") || "")
				});
			},
			children: [
				/* @__PURE__ */ jsx(Field, {
					label: "تاریخ شمسی",
					children: /* @__PURE__ */ jsx(Input, {
						name: "date",
						defaultValue: settings.today,
						required: true
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "تأمین‌کننده",
					children: /* @__PURE__ */ jsx(Select, {
						name: "supplier",
						required: true,
						children: suppliers.map((s) => /* @__PURE__ */ jsx("option", { children: s.name }, s.id))
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "نوع حبوبات",
					children: /* @__PURE__ */ jsx(Select, {
						name: "product",
						children: PRODUCTS.map((p) => /* @__PURE__ */ jsx("option", { children: p }, p))
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "مقدار (کیلو)",
					children: /* @__PURE__ */ jsx(Input, {
						name: "kg",
						type: "number",
						step: "0.1",
						required: true
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "قیمت هر کیلو",
					children: /* @__PURE__ */ jsx(Input, {
						name: "price",
						type: "number",
						required: true
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "هزینه حمل",
					children: /* @__PURE__ */ jsx(Input, {
						name: "freight",
						type: "number",
						defaultValue: 0
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "کیفیت",
					children: /* @__PURE__ */ jsx(Select, {
						name: "quality",
						children: QUALITY.map((q) => /* @__PURE__ */ jsx("option", { children: q }, q))
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "وضعیت تأیید",
					children: /* @__PURE__ */ jsx(Select, {
						name: "status",
						children: APPROVAL.map((q) => /* @__PURE__ */ jsx("option", { children: q }, q))
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "توضیحات",
					children: /* @__PURE__ */ jsx(Input, { name: "note" })
				})
			]
		}),
		/* @__PURE__ */ jsx(DataTable, {
			columns: [
				{
					key: "d",
					label: "تاریخ"
				},
				{
					key: "s",
					label: "تأمین‌کننده"
				},
				{
					key: "p",
					label: "محصول"
				},
				{
					key: "k",
					label: "کیلو"
				},
				{
					key: "pr",
					label: "قیمت"
				},
				{
					key: "t",
					label: "مبلغ + حمل"
				},
				{
					key: "q",
					label: "کیفیت"
				},
				{
					key: "st",
					label: "وضعیت"
				}
			],
			rows: purchases.map((p) => ({
				id: p.id,
				onDelete: () => remove("purchases", p.id),
				cells: [
					p.date,
					p.supplier,
					p.product,
					num(p.kg, 1),
					toman(p.price),
					toman(purchaseTotal(p)),
					p.quality,
					p.status
				]
			}))
		})
	] });
}
//#endregion
export { Page as component };
