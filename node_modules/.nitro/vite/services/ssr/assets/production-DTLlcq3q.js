import { _ as packLabel, c as PACKS, g as num, p as useWorkshop, r as PageTitle, u as PRODUCTS, v as pct } from "./router-BUaE4XGK.js";
import { n as Input, r as Select, t as Field } from "./input-CHwMk_IU.js";
import { n as DataTable, t as AddForm } from "./journal-KWw6p0QH.js";
import { _ as wasteKg, u as packCount } from "./kpis-CvtxtOcE.js";
import { jsx, jsxs } from "react/jsx-runtime";
//#region src/routes/production.tsx?tsr-split=component
function Page() {
	const { production, employees, addProduction, remove, settings } = useWorkshop();
	return /* @__PURE__ */ jsxs("div", { children: [
		/* @__PURE__ */ jsx(PageTitle, {
			title: "تولید و بسته‌بندی",
			hint: "ضایعات، درصد و تعداد بسته خودکار محاسبه می‌شود. وزن بسته: ۰.۴ / ۰.۹ / ۱۰ کیلو."
		}),
		/* @__PURE__ */ jsxs(AddForm, {
			title: "ثبت تولید",
			onSubmit: (e) => {
				const f = new FormData(e.currentTarget);
				addProduction({
					date: String(f.get("date") || settings.today),
					product: String(f.get("product")),
					inputKg: Number(f.get("inputKg")),
					cleanKg: Number(f.get("cleanKg")),
					packKg: Number(f.get("packKg")),
					operator: String(f.get("operator")),
					note: String(f.get("note") || "")
				});
			},
			children: [
				/* @__PURE__ */ jsx(Field, {
					label: "تاریخ شمسی",
					children: /* @__PURE__ */ jsx(Input, {
						name: "date",
						defaultValue: settings.today,
						required: true
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "محصول",
					children: /* @__PURE__ */ jsx(Select, {
						name: "product",
						children: PRODUCTS.map((p) => /* @__PURE__ */ jsx("option", { children: p }, p))
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "مقدار ورودی (کیلو)",
					children: /* @__PURE__ */ jsx(Input, {
						name: "inputKg",
						type: "number",
						step: "0.1",
						required: true
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "مقدار پاک‌شده (کیلو)",
					children: /* @__PURE__ */ jsx(Input, {
						name: "cleanKg",
						type: "number",
						step: "0.1",
						required: true
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "وزن بسته",
					children: /* @__PURE__ */ jsx(Select, {
						name: "packKg",
						children: PACKS.map((p) => /* @__PURE__ */ jsx("option", {
							value: p,
							children: packLabel(p)
						}, p))
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "اپراتور",
					children: /* @__PURE__ */ jsx(Select, {
						name: "operator",
						children: employees.map((e) => /* @__PURE__ */ jsx("option", { children: e.name }, e.id))
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "توضیحات",
					children: /* @__PURE__ */ jsx(Input, { name: "note" })
				})
			]
		}),
		/* @__PURE__ */ jsx(DataTable, {
			columns: [
				{
					key: "d",
					label: "تاریخ"
				},
				{
					key: "p",
					label: "محصول"
				},
				{
					key: "i",
					label: "ورودی"
				},
				{
					key: "c",
					label: "پاک‌شده"
				},
				{
					key: "w",
					label: "ضایعات"
				},
				{
					key: "pct",
					label: "درصد"
				},
				{
					key: "pk",
					label: "بسته"
				},
				{
					key: "n",
					label: "تعداد"
				},
				{
					key: "o",
					label: "اپراتور"
				}
			],
			rows: production.map((p) => ({
				id: p.id,
				onDelete: () => remove("production", p.id),
				cells: [
					p.date,
					p.product,
					num(p.inputKg, 1),
					num(p.cleanKg, 1),
					num(wasteKg(p), 1),
					pct(p.inputKg ? wasteKg(p) / p.inputKg : 0),
					packLabel(p.packKg),
					packCount(p),
					p.operator
				]
			}))
		})
	] });
}
//#endregion
export { Page as component };
